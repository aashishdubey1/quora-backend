const express = require('express');

const router = express.Router()

router.post('/',()=>{/* Create like (toggle) */})

router.delete('/',()=>{/* remove like */})

router.get('/',()=>{/* get likes count */})

module.exports = router